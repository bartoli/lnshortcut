#ifndef MAINTHREAD_HPP
#define MAINTHREAD_HPP

/*
 * Class for the Main thread.
 * this is not actually a thread class, just the class that is run from main()
*/
class MainThread
{
public:
  int run();
};

#endif // MAINTHREAD_HPP

